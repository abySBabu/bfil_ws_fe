apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bfil-fe.fullname" . }}-test-connection"
  labels:
    {{- include "bfil-fe.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bfil-fe.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
